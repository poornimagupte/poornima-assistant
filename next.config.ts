import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Slice 1 is intentionally minimal. Image domains for book/crochet
  // cover art get added when those widgets land.
};

export default nextConfig;
